import {
  StyleSheet,
  Text,
  View,
  Image,
  useWindowDimensions,
} from "react-native";
import React from "react";

import fonts from "../config/fonts";
import colors from "../config/colors";

export default function TabTestScreen() {
  return <Text style={fonts.title}>Teast screen</Text>;
}

const styles = StyleSheet.create({});
